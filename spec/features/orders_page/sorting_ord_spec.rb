require 'rails_helper'

describe 'Sorting orders', type: :feature do
  # let!(:delivery) { create(:delivery, method: FFaker::Lorem.word) }
  # let!(:user) { create(:user) }
  # let!(:order1) { create(:order, :complete_step, user_id: user.id, delivery_id: delivery.id) }
  # let!(:order2) { create(:order, :in_delivery_step, user_id: user.id, delivery_id: delivery.id) }
  # let!(:order3) { create(:order, :delivered_step, user_id: user.id, delivery_id: delivery.id) }
  # let!(:order4) { create(:order, :canceled_step, user_id: user.id, delivery_id: delivery.id) }
  # 
  # before do
  #   login_as(user, scope: :user)
  #   visit orders_path
  # end
  #
  # it 'show orders page' do
  #   pry
  #   expect(page).to have_content I18n.t('orders.my_orders')
  #   expect(page).to have_content 'In progress'
  # end

end
