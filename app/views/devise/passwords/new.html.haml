%main.container.general-main-wrap
  .text-center
    %h1.mb-30 Forgot your password?
    %p.general-password-text We'll email you instructions on how to reset it
  %div.general-form
    = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
      .form-group.mb-25
        = f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "Enter your email", class: 'form-control'
      %button.btn.btn-default.center-block.mb-20
        = f.submit "Email instructions", class: 'email-constructions'
      .text-center
        %a.in-gold-500.mb-20{href: "/users/sign_in"} Cancel
