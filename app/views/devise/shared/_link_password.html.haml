- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations'
  = link_to t('devise.forgot_password'), new_password_path(resource_name), class: 'default-orange'
  %br/
